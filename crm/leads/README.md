# crm/leads — Leads

Leads de vendas do tenant: tabela `crm_leads` com `tenant_id`, `contact_id`,
`stage`, `value`, `origin`, `owner_user_id`, `probability`, `notes`.

**Quando implementar:** Fase 5 (CRUD) e Fase 8 (auditoria de eventos de lead).
Nada de código aqui ainda.